pattern = r"\{ path: '([^']*)',\s*component: (\w+)"

# Find all matches
matches = re.findall(pattern, module_code)

# Convert matches to a list of dictionaries
routes = [{'path': match[0], 'component': match[1]} for match in matches]

print(routes)
