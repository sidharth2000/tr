pattern = r'(\w+)\.(\w+)\(([^)]*)\)'

# Find all matches in the C++ code
matches = re.findall(pattern, cpp_code)

# Process and print the results
for match in matches:
    objname, funcname, arglist = match
    print(f"Object Name: {objname}, Function Name: {funcname}, Argument List: {arglist}")
